# getbased-rag — moved

This repository has been merged into the **[getbased-agents](https://github.com/elkimek/getbased-agents)** monorepo.

- **New location:** `packages/rag/` in [getbased-agents](https://github.com/elkimek/getbased-agents/tree/main/packages/rag)
- **Old commit history:** preserved via `git subtree add` — nothing lost
- **PyPI package name (`getbased-rag`):** unchanged. `pipx install getbased-rag` still works as before.
- **Issues + PRs:** please file in the new repo.

This repo is archived and no longer accepting contributions.
